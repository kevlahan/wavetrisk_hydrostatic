test_case          drake         ! name of test case
run_id             drake_2layer  ! name of run (for saved data)
max_level	   8             ! maximum level of resolution
level_fill	   5             ! make all points active up to level_level (accelerates elliptic solver)	
zlevels            2             ! number of vertical levels
remap              F             ! remap vertical grid
iremap             10            ! remap every iremap time steps
tol_elliptic       1e-11          ! tolerance for elliptic solver 
coarse_iter        500           ! maximum number of elliptic solver bicgstab iterations at coarsest level
fine_iter          500           ! maximum number of elliptic solver jacobi iterations at finer levels
log_iter           T             ! print elliptic solver relative residual error
default_thresholds T             ! use default tolerances (T) or calculate tolerances dynamically (F)
tol      	   1e-2          ! non-dimensional tolerance
cfl_num            20.0          ! cfl number for barotropic mode
adapt_dt           T             ! adapt time step
dt_write           5.0           ! save interval (days)
CP_EVERY           4             ! checkpoint every CP_EVERY saves
time_end           1000          ! final time (days)
resume            -1             ! restart from checkpoint (negative means fresh restart)
alpha              1e-1          ! porosity penalization parameter
drag               T             ! use bottom drag if T
npts_penal         4             ! number of grid points over which to smooth mask
resolution         1.5           ! number of grid points resolving Taylor scale (determines viscosity)

