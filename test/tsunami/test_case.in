test_case          tsunami       ! name of test case
run_id             tsunami       ! name of run (for saved data)
max_level	   10            ! maximum level of resolution
zlevels            1             ! number of vertical levels
default_thresholds F             ! use default tolerances (T) or calculate tolerances dynamically (F)
tol      	   6e-2          ! non-dimensional tolerance
dt_write           30            ! save interval (minutes)
CP_EVERY           6             ! checkpoint every CP_EVERY saves
time_end           6             ! final time (hours)
resume            -1             ! restart from checkpoint (negative means fresh restart)
